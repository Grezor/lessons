//addition : 
function getRandomInt(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min)) + min; //The maximum is exclusive and the minimum is inclusive
}
/**
 * prendre un chiffres max et min (math.random)
 * vérifier que le max et plus grand que le min n'est pas egale a 0
 * vérifier que le resultat n'est pas égale a 0
 * max + min = result
 * vérifier le resultat taper avec le resultat des deux
 */
function addition(min, max){

}

// multiplication :

/**
 *  cree une function multiplication (max, min)
 *  crée un tableau1 max
 *  crée un tableau min 
 *  parcourir le tableau 1 et prendre un chiffre aléatoire et faire pareil pour le tableau deux
 *  mettre les deux chiffres dans une variable multiplications
 *  
 *  
 */