// en math : une valeur qui peut prendre duex états
// en informatique : un boolean est modélisé par soit :
// en binaire etat 1 : 0 ou etat 2 : 1
// Etat1, valeur 0, état 2: toute autre valeur != 0 (général 1)
// avec le type booléen, état 1: false, état 2: true

let a = true;
let b = true;

// et 
console.log(a & b);
console.log(a && b);

// ou
console.log(a | b);
console.log(a || b);

// xor, nor

//not, inversion 
console.log(a | b);

//not négation
